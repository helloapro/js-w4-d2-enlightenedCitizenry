<div class="row">
  <div class="col-sm-4 well bill-well">
    <ul>
      {{#each model.recentBills as |bill|}}
        <li>{{bill-list bill=bill}}</li>
        <hr>
      {{/each}}
    </ul>
  </div>

  <div class="col-sm-4 well bill-well">
    <ul>
      {{#each model.houseBills as |bill|}}
        <li>{{bill-list bill=bill}}</li>
        <hr>
      {{/each}}
    </ul>
  </div>

  <div class="col-sm-4 well bill-well">
    <ul>
      {{#each model.senateBills as |bill|}}
        <li>{{bill-list bill=bill}}</li>
        <hr>
      {{/each}}
    </ul>
  </div>

</div>
